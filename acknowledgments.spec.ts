describe('Acknowledge messages when necessary', () => {
  it('does not acknowledge without the fossil extension', () => {

  })

  it('does not acknowledge if the consumer is not in the list', () => {

  })

  it('does acknowledge if the consumer is part of the list', () => {

  })
});

describe('Named consumer', () => {
  it('streams events from the consumer stream URL', () => {

  });

  it('sends the CloudEvent to the handler', () => {

  });

  it('automatically commits the last known event number regularly', () => {

  });
});
